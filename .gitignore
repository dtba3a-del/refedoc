# Производное от прогонов — собирается заново, в публичную зону не кладётся.
sliced_output/
_test/
*.pyc
__pycache__/

# Граница: сюда не попадает даже случайно (крючок это же и запрещает).
# inbox/ в этом списке НЕТ намеренно: с 2026-09-03 это лоток автора.
chatlog/
sessions/
context-export/
*.jsonl
.env
