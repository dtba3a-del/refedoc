# Ответ на уведомление 2026-09-13 «Баг в bootstrap.ps1 и kit-windows.yml»

**Дата:** 2026-09-18 02:1x UTC
**Статус:** проверено по первоисточнику (журналы GitHub Actions), починено, CI зелен целиком
**Основание:** `list_workflow_jobs` и `get_job_logs` по каждому прогону workflow `kit-selftest`; коммиты `65daa08`, `233192b`, `84e5614`, `d18b1a2` в `main`

## Что показали журналы `[внешний источник]`

* У всех **7** прогонов с 11.09 по 13.09 (уведомление говорит о 6) — **ноль джобов**: `created_at == run_started_at == updated_at`, падение в ту же секунду. Ни один шаг ни разу не исполнялся, `bootstrap.ps1` в CI не запускался.
* Причина — сам файл workflow: имя шага `bootstrap-windows` несло «: » без кавычек, и файл был недействителен как YAML (`pyyaml`: «mapping values are not allowed here», строка 25, столбец 73). «28 ошибок в 6 прогонах» логами не подтверждаются: логов шагов у этих прогонов нет вовсе.

## Четыре правки уведомления — замер по каждой

1. `return` вместо `throw` при недокачанном файле (bootstrap.ps1:25) — **не применено**: скрипт нарочно не пользуется `exit`/`throw` — под `irm … | iex` это закрывает окно консоли (комментарий в шапке скрипта, строка 9). В настоящем прогоне (прогон 9 и далее) все 8 файлов скачались, ветка не срабатывала.
2. `$pyargs` «передаётся неправильно» (bootstrap.ps1:56–57) — **не подтверждено**: сплат `@pyargs` в строке 60 отработал, шаг `probe` был запущен (`python.exe … run_local.py --only probe`, прогон 9, job `bootstrap-windows`).
3. Строка 60 — **не применено**, см. п. 2.
4. Одинарные кавычки в последней `Write-Host` (bootstrap.ps1:63) — **не применено**: строка печатает команду для копирования пользователем, интерполяция там не нужна; `$exe` и `$dir` в ней раскрываются (двойные кавычки снаружи), одинарные — внутри команды.

Итого из четырёх правок к причине падений не относится ни одна.

## Что было починено на самом деле `[вычислено здесь]`

1. `65daa08` — `kit-windows.yml`: имя шага взято в кавычки; поведение шагов не менялось. Прогон 8: впервые появились джобы — ubuntu 3.12/3.13 зелены, windows 3.12/3.13 красны на первой же кириллической строке самопроверки (`UnicodeEncodeError`, stdout — cp1252).
2. `233192b` — `selftest.py`: stdout/stderr переводятся на UTF-8 при старте. Прогон 9: windows 7 из 9, красны проверки 7 (TEMP у runner'а — короткое имя `RUNNER~1` против `resolve()` комплекта) и 10 (ребёнок шага пишет в лог с кодовой страницей консоли).
3. `84e5614` — `run_local.run()` передаёт ребёнку `PYTHONIOENCODING=utf-8` принудительно (лог читается как UTF-8); `selftest.py` берёт временную папку развёрнутой. Прогон 10: selftest 4 × 9/9 — первая зелёная самопроверка на Windows за всё время workflow.
4. `d18b1a2` — `bootstrap.ps1`, `Find-Python`: `python` с PATH первым, затем `py -3`. Первый настоящий прогон `bootstrap-windows` (прогон 9, 12 мин 37 с) взял через `py -3` системный Python 3.14.7 мимо подготовленного 3.12; pip поставил под 3.14 numpy сборки MINGW-W64 («CRASHES ARE TO BE EXPECTED»), и `probe` упал кодом 3221225477 (0xC0000005) — `host_log.json` не создан. Прогон 11: `bootstrap-windows` зелен за 4 мин 26 с, все пять джобов зелены.

Попутно, из того же среза: `env/Copilot/scripts/backup-inversepolar.bat` — запасная ветка `git clone --depth 50` шла без `--unshallow` и при отказе `gh` оставляла срез в 50 коммитов вместо бэкапа; дотягивание истории добавлено (`0b9f753`).

## Что осталось за границей

* Клон приватного `InvesePolar` внутри CI отказывает кодом 128 (в CI нет входа git) — комплект об этом честно говорит в отчёте развёртывания; шаг `data` в CI не проверяется, только `probe`.
* Python 3.14 на пользовательской машине с numpy сборки MINGW-W64 даст тот же 0xC0000005 в `probe` — комплект под 3.14 не заявлен; правило выбора Python теперь предпочитает интерпретатор оболочки.
